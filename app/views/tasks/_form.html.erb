<%= form_for task, remote: true do |f| %>
  <%= f.text_field :description, placeholder: "Add a task. Press enter when finished.", id: 'description-field', size: '50' %>
  <%= f.hidden_field :user_id, value: user.id %>
<% end %>
